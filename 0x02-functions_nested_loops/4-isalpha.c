#include "main.h"


/**
 * print_alphabet - function that prints alphabet in lower case
 *
 * Return: always 0
 */

	int _isalpha(int c)
	{
		return ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'));
	}

